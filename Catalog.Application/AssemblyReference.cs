namespace Catalog.Application
{
    // Clase vacía usada solo para sacar el Assembly de Application
    // desde otros proyectos (para registrar MediatR, etc).
    public static class AssemblyReference { }
}
